import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input: {
        backgroundColor: '#fff',
        width: '80%',
        borderRadius: 5,
        margin: 10,
        fontSize: 20,
        textAlign: 'center',
        fontFamily: 'courier new',
        fontWeight: 'bold'
    }
});